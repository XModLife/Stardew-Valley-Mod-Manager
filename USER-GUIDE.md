[**简体中文**](USER-GUIDE.md) | [English](USER-GUIDE.en.md)

# Stardew Valley Mod Manager 使用手册

适用版本：Stardew Valley Mod Manager 1.0.0 及后续版本，直至本手册更新。

Stardew Valley Mod Manager（以下简称“SVMM”）是一款 macOS 本地 Mod 管理工具。它读取并管理您现有的 Stardew Valley Mods，不会为游戏创建另一套独立 Mod 环境。多数管理数据保存在本机。

## 目录

- [Stardew Valley Mod Manager 使用手册](#quick)
- [目录与扫描](#setup)
- [界面语言](#language)
- [概览](#overview)
- [Mod 库](#library)
- [分类与配置方案](#organization)
- [更新与 Nexus Mods](#updates)
- [依赖与获取](#dependencies)
- [诊断与存储](#diagnostics)
- [存档概览](#save)
- [问题排查与反馈](#troubleshooting)

---

<a id="quick"></a>
## Stardew Valley Mod Manager 使用手册

从首次配置到日常 Mod 管理的基本流程。

> **理解 SVMM 的角色**  
> SVMM 负责整理、检查和执行用户要求的 Mod 管理操作。它不替代 Stardew Valley、SMAPI 或第三方 Mod，也不能保证第三方 Mod 的兼容性。

### 推荐的日常使用方式

1. 打开 SVMM，确认概览中的 Mod 状态。
2. 如果曾在 Finder 或其他工具中修改 Mods 目录，先重新扫描 Mod。
3. 需要时调整分类、配置方案或 Mod 启用状态。
4. 出现更新、缺少依赖或依赖版本问题时，先打开 Mod 详情确认来源、版本和依赖状态，再决定更新、获取依赖或手动处理。

<a id="setup"></a>
## 目录与扫描

让 SVMM 找到正确的 Stardew Valley Mods，并保持列表与磁盘一致。

### 设置 Mods 目录

首次使用时，请在“设置”中选择 Stardew Valley 的 Mods 目录。SVMM 需要对该目录具有相应文件访问权限，才能读取 Mod 信息并执行启用、停用、更新或删除等操作。

1. 打开“设置”，找到游戏 / Mods 目录设置。
2. 通过 macOS 文件选择窗口选择实际使用的 Mods 目录。
3. 返回主界面，确认 Mod 能被正常扫描并显示。

> **关于 macOS 文件权限**  
> 通过系统文件选择器主动选择目录，是向应用授予相应本地文件访问权限的一部分。若目录被移动、权限被撤销或磁盘状态发生变化，可能需要重新选择。

### 重新扫描 Mod

使用“工具 > 重新扫描 Mod”可重新读取当前 Mods 目录。重新扫描用于同步 SVMM 当前显示与磁盘上的实际文件状态；扫描本身不会因为发现问题就自动删除正常 Mod。

- 在 Finder 中手动添加、删除或移动了 Mod。
- 使用其他工具修改了 Mods 目录。
- SVMM 当前列表与实际目录内容明显不一致。


<a id="language"></a>
## 界面语言

SVMM 支持 **English**、**简体中文**和**跟随系统**。

语言属于应用启动配置。选择新的语言后，SVMM 会保存待应用语言，并提示是否重新启动。

- **重新启动**：立即重新打开 SVMM，让应用内容与 macOS 原生菜单一起使用新语言。
- **稍后**：当前进程继续使用本次启动时的语言；下次完全退出并重新打开 SVMM 时应用所选语言。
- 如果重新选择当前启动时正在使用的语言，会取消待应用的语言变更。
- **跟随系统**：移除 SVMM 自己的语言覆盖，下次启动时由 macOS 决定应用语言。

这样可以避免运行过程中出现部分中英混排。

<p align="center">
  <img src="docs/images/zh-CN/settings.png" alt="语言与设置" width="94%">
</p>

<a id="overview"></a>
## 概览

<p align="center"><img src="docs/images/zh-CN/overview.png" alt="概览" width="96%"></p>


快速判断当前 Mod 环境是否需要进一步处理。

概览汇总 Mod 数量和主要状态，并提供农场存档摘要。状态卡片用于导航和快速判断，不应被理解为对第三方 Mod 安全性或兼容性的绝对结论。

### 已启用

表示当前 Mod 处于 SVMM 所识别的启用状态。

### 需更新

表示根据当前可获得的更新信息发现较新版本。是否安装仍应结合作者说明、游戏版本、SMAPI 和依赖关系判断。

### 需处理

表示存在需要用户确认的扫描、依赖、结构或其他管理问题。缺少依赖不等于一定要立即安装；先打开相关 Mod 详情查看是必需依赖、可选依赖、已停用、版本过低还是无法唯一判断，再决定处理方式。

### 报错

表示扫描、解析、文件操作或相关流程出现异常。错误信息应作为诊断线索，不建议仅凭“报错”状态直接删除 Mod。

> **状态是辅助信息**  
> SVMM 可以帮助缩小问题范围，但 Mod 是否适合当前存档、是否与其他 Mod 冲突，仍取决于第三方 Mod 本身和您的实际环境。

<a id="library"></a>
## Mod 库

| 卡片视图 | 列表视图 |
| --- | --- |
| ![卡片视图](docs/images/zh-CN/mod-library-card.png) | ![列表视图](docs/images/zh-CN/mod-library-list.png) |


查看、筛选、选择和管理已经扫描到的 Mod。

Mod 库是 SVMM 的主要操作区域。卡片视图和列表视图只改变显示方式，不会产生两套不同的 Mod 数据。

### 两种查看方式

- **卡片视图**：更适合快速浏览。可用时显示 Nexus 缩略图；没有图片或离线时使用安全回退显示。
- **列表视图**：更适合比较名称、版本、类型、依赖、状态和安装时间等结构化信息，并可进行排序。

### 常用操作

- 通过“Mod 状态”筛选当前需要查看的 Mod。
- 选择 Mod 后查看详细信息、版本、Unique ID、依赖和更新来源。依赖区域会显示当前状态，并在能够解析可靠来源时提供“获取依赖”“获取新版本”或“查看页面”等操作。
- 使用名称备注记录更易理解的个人名称。
- 通过右键菜单进行分类、加入配置方案、打开详情或删除等操作。
- 对高影响操作（例如删除或批量更新）先确认对象和范围。

### 名称备注不会修改 Mod

名称备注属于 SVMM 的本地管理信息。例如可将 “Elle's Seasonal Buildings” 备注为“季节建筑美化”。该操作不会改写 Mod 原始名称或 manifest.json。

> **停用不等于删除**  
> 停用用于暂时不让 Mod 参与正常加载；删除则是移除对应 Mod 文件。排查冲突或临时切换玩法时优先考虑停用，确认不再需要后再删除。

### macOS 多选操作

- **单击**：选择一个 Mod。
- **⌘ + 单击**：进行非连续多选。
- **⇧ + 单击**：选择连续范围。
- **⌘A**：选择当前列表中的全部 Mod。

<a id="organization"></a>
## 分类与配置方案

| 分类 | 配置方案 |
| --- | --- |
| ![分类](docs/images/zh-CN/categories.png) | ![配置方案](docs/images/zh-CN/profiles.png) |


一个负责整理，一个负责决定实际使用的 Mod 组合。

### 分类

分类用于组织和查找 Mod。它属于 SVMM 管理数据，不会改变 Mod 作者定义的类型，也不会自动改变 Mod 的启用状态。

- 系统默认分类方案可作为只读参考。
- 用户可创建和维护自己的可编辑分类方案。
- Mod 库和分类页面使用同一套分类关系。

### 配置方案

配置方案用于保存一组实际启用 / 停用状态，以便在不同玩法或测试环境之间切换。主界面显示的是当前实际应用结果，而不是历史数据库记录数。如果方案中保存的成员已经从 Mods 文件夹移除，SVMM 会将其作为无法匹配的历史记录提示；若这些 Mod 已经永久删除，可前往“设置 → 清理已卸载 Mod 的残留管理记录”清除相应方案成员记录。

> **最简单的判断方法**  
> 分类 = “怎么整理”；配置方案 = “这次实际用什么”。不要把分类当作启用规则，也不要把配置方案当作标签系统。

<a id="updates"></a>
## 更新与 Nexus Mods

### Nexus 更新流程

| 免费账户流程 | Premium 批量更新 |
| --- | --- |
| ![免费账户更新](docs/images/zh-CN/update-free.png) | ![Premium 批量更新](docs/images/zh-CN/update-premium-batch.png) |


检查更新来源，并在支持的条件下完成下载与安装。

SVMM 会利用 Mod 元数据以及 SMAPI、Nexus Mods 等第三方服务提供的信息检查部分 Mod 的更新状态。更新和依赖获取会复用部分 Nexus Mods 下载能力，但它们是两类不同任务：更新针对已经安装的 Mod，依赖获取用于解决另一个 Mod 声明的缺失或版本异常依赖。结果取决于 Mod 元数据、第三方服务和可用文件。

> **“无法检查”不等于 Mod 有问题**  
> 可能只是没有有效 Update Key、来源已失效、第三方服务暂时不可用，或该来源当前不能自动识别。只要 Mod 本身能被游戏正常加载，它仍可能正常使用。

### 连接 Nexus Mods（可选）

Nexus Mods 连接不是使用 SVMM 本地管理功能的前提。连接后，SVMM 可以在受支持的情况下获取 Nexus 元数据、文件信息、缩略图和下载信息。

- **Free 账户**：需要从 Nexus Mods 下载更新或依赖时，Free 账户通常通过浏览器完成 NXM 授权，再由 SVMM 接续处理授权后的下载、校验和受支持的安装流程。
- **Premium 账户**：对于符合条件的更新或依赖，Premium 账户可由 SVMM 获取直接下载链接。Premium 还可使用批量更新和符合条件的批量依赖获取；实际写入 Mods 仍经过本地校验并按安全事务顺序执行。

> **Nexus API 凭据**  
> SVMM 将 Nexus API 凭据保存在 macOS 钥匙串（Keychain）。不要把 API Key 放进公开 Issue、截图、日志或发送给其他用户。

### 更新前建议

1. 确认待更新的 Mod 和目标版本。
2. 确认更新来源正确，尤其是曾手动修正来源的 Mod。
3. 大型内容 Mod 或重要存档环境更新前阅读作者说明。
4. 重要环境保留独立备份，不把临时更新备份当作长期备份方案。

<a id="dependencies"></a>
## 依赖与获取

### 依赖获取流程

| 免费账户 | Premium 批量获取 |
| --- | --- |
| ![免费账户获取依赖](docs/images/zh-CN/dependencies-free.png) | ![Premium 批量获取依赖](docs/images/zh-CN/dependencies-premium-batch.png) |


理解依赖状态，并在支持时查找来源、下载、验证和安装。

SVMM 会读取 Mod 的 Manifest 依赖声明，并将声明与当前 Mods 文件夹中的实际 Mod 状态进行比较。它可以帮助您判断依赖是否存在、是否被停用、版本是否过低，并在部分情况下解析可靠来源和完成获取。依赖判断是管理辅助信息，不代替 Mod 作者的安装说明。

### 先理解依赖声明

- **必需依赖**：父 Mod 明确声明为必需。缺失、停用或版本不满足时，父 Mod 可能无法加载或功能不完整，应优先查看作者说明。
- **可选依赖**：父 Mod 声明为可选。缺失可选依赖通常不表示父 Mod 必然损坏，它可能只影响特定联动功能。

### 常见依赖状态

- 缺失：当前没有检测到可与声明 Unique ID 对应的已安装 Mod。
- 已安装但停用：依赖文件存在，但当前不是启用状态。通常先启用现有依赖，而不是重复下载。
- 版本过低：检测到已安装依赖，但其版本低于父 Mod 声明的最低版本。
- 版本无法确认：依赖存在，但当前元数据不足以可靠比较版本；不要把它直接等同于“必须更新”。
- 重复 Unique ID：当前存在多个可能对应的 Mod，SVMM 无法可靠确定哪个实例应当满足依赖。

### 依赖来源解析

对于缺失、版本过低或版本无法确认的依赖，SVMM 可以通过 SMAPI 查询 Unique ID 对应的公开元数据，并优先采用明确返回的项目页面。可能识别 Nexus Mods、GitHub、CurseForge、ModDrop 或其他来源。无法可靠解析时，SVMM 不会猜测下载地址，而是允许复制 Unique ID 供您自行搜索。

> **找到来源不等于文件一定适合**  
> 来源解析只回答“这个依赖通常在哪里发布”。具体应该安装哪个文件、是否需要旧版、额外前置或特殊安装步骤，仍应以 Mod 作者说明为准。

### 获取单个依赖

1. 在 Mod 详情的依赖区域找到缺失或版本异常的依赖，先确认它是否为必需依赖以及最低版本要求。
2. 等待 SVMM 解析来源。Nexus Mods 的明确 Mod 页面可进入应用内依赖获取流程；其他来源通常打开对应项目页面。
3. 缺失的 Nexus 依赖可以使用“获取依赖”；已安装但版本过低的 Nexus 依赖会复用 Mod 更新流程。
4. 选择受支持的 Nexus 文件。若页面存在多个可能的 MAIN ZIP，单个获取流程会要求您明确选择，而不会随意猜测。
5. Free 账户通过浏览器完成 NXM 授权；Premium 账户在符合条件时可直接在 SVMM 内获取下载链接。
6. 下载后，SVMM 会在临时目录解压并验证 Manifest、Unique ID、目标目录和现有 Mod 身份冲突。验证失败时不会把包写入 Mods。
7. 确认安装后，SVMM 执行本地安装事务并重新扫描 Mods。重新检查父 Mod 的依赖区域，确认问题是否真正解决。

### Nexus Mods 账户差异

- **Free**：依赖获取需要浏览器参与 NXM 授权。授权完成后，SVMM 接续临时下载、校验和安装流程。
- **Premium**：符合条件时可直接获取下载链接，并支持批量依赖获取。账户仍受 Nexus Mods API Rate Limit、文件可用性和平台规则限制。

> **安装前的本地安全检查**  
> 自动依赖安装只处理受支持的 ZIP。SVMM 不会覆盖已经安装的相同 Unique ID Mod；对于包含多个 Mod 的 ZIP，只有在能够证明它们属于同一 bundled package 时才作为一个事务安装。游戏运行中不会提交依赖安装。RAR / 7z 当前不进入自动安装流程。

### Premium 批量依赖获取

当多个 Mod 同时存在可处理的 Nexus 依赖问题时，Premium 账户可以批量处理符合自动化条件的项目。批量功能采用保守规则，不会为了追求“全部自动”而猜测文件。

- 按 Unique ID 去重，并优先处理必需依赖。
- 只自动处理来源明确为 Nexus Mods、且能够确定唯一受支持 MAIN ZIP 的依赖；不满足条件的项目会跳过并说明原因。
- 缺失依赖使用新增安装事务，版本过低依赖使用现有更新事务。
- 对 Mods 的写入按队列串行执行，不并发提交多个文件事务；全部完成后最多重新扫描一次。
- 停止批量任务时，网络请求和正在下载的项目可以取消；正在进行最终文件写入的项目不会被强制中断，以避免留下不明确的安装状态。

> **当前边界：不递归安装依赖树**  
> SVMM 只处理当前正在查看或批量队列中明确列出的依赖。新安装的依赖如果自己还有进一步依赖，SVMM 不会在同一操作中自动继续递归下载。安装完成并重新扫描后，请再次检查依赖状态。这个边界用于减少错误来源解析和不可控的连锁安装。

<a id="diagnostics"></a>
## 诊断与存储

先理解问题，再决定是否需要修改或删除文件。

### 扫描诊断

“工具 > 扫描诊断”用于查看扫描 Mods 时发现的结构、Manifest 或解析问题，并解释某些 Mod 为什么没有被正常识别或为什么状态无法判断。

- 先阅读具体问题描述和涉及的 Mod。
- 需要时在 Finder 中检查对应目录和 manifest.json。
- 修正外部文件后重新扫描，确认问题是否消失。

> **诊断问题不是自动判定“Mod 已损坏”**  
> 某些旧 Mod、特殊目录结构或非标准 Manifest 可能触发扫描诊断。依赖问题则来自 Mod 自己声明的依赖关系和当前安装状态，两者不要混为一谈。应结合 SMAPI、游戏实际运行、依赖详情和 Mod 作者说明判断。

### 存储与缓存

在“设置 > 存储与缓存”中可以查看 SVMM 管理的缓存和更新事务数据，并执行针对性的清理。

- 清除 Nexus 缩略图缓存不会卸载 Mod。
- 清理已完成或遗留的更新事务数据用于释放由 SVMM 管理的临时空间。
- “清理已卸载 Mod 的残留管理记录”针对的是 SVMM 自己的备注、分类等残留记录，不是清理 Stardew Valley 游戏文件。

<a id="save"></a>
## 存档概览

在概览中查看本地 Stardew Valley 存档的只读摘要。

SVMM 可以读取本地 Stardew Valley 存档中的部分信息，用于显示农场状态。此功能用于快速了解当前存档，不参与 Mod 启用、更新或配置方案逻辑。

- 玩家和农场名称。
- 游戏日期、游玩时间和金币等摘要。
- 配偶、子女、房屋升级等游戏状态。
- 游戏版本和可读取的进度信息。

> **只读访问**  
> SVMM 的存档概览以只读方式解析存档，不会因为查看农场状态而修改存档内容。

<a id="troubleshooting"></a>
## 问题排查与反馈

优先保留现场信息，按顺序缩小问题范围。

### 推荐排查顺序

1. 确认当前选择的 Stardew Valley / Mods 目录正确且可以访问。
2. 执行“工具 > 重新扫描 Mod”。
3. 打开“扫描诊断”，查看具体文件结构或 Manifest 问题。
4. 如果问题与依赖有关，打开 Mod 详情确认依赖 Unique ID、是否必需、最低版本、当前状态和解析出的来源；不要仅根据“缺少依赖”字样盲目安装文件。
5. 仍无法判断时，再提交 SVMM 问题反馈，并提供可复现步骤。

> **避免把“重装一切”作为第一步**  
> 不要在尚未确认原因时直接清空 Mods、删除全部管理数据或重装游戏。高影响操作前先保留必要备份和错误信息。

### 提交问题时建议包含

高质量的问题描述应足以让开发者重复操作路径，而不是只有“不能用”或一张没有上下文的截图。

- SVMM 版本与 macOS 版本。
- Stardew Valley 和 SMAPI 版本。
- 相关 Mod 名称、版本及必要的 Unique ID。
- 从正常状态开始的具体操作步骤、实际结果和预期结果。
- 必要的错误文字或经过检查的诊断信息。

> **不要公开认证秘密**  
> 不要在公开 Issue、截图或普通邮件中提供 Nexus API Key、Token、支付密码、账户密码或其他认证凭据。安全、隐私或不适合公开的问题可发送至 SVMM@npccare.cn。

SVMM@npccare.cn
